#include "stdafx.h"
#include<iostream>


void maxsum(char *a[], char *b[], char *c[]);
void main() {
	char a[50], b[50], c[100];
	cin >> a >> b >> c;
}
void maxsum(char *a[], char *b[], char *c[]) {
	int i;
	for (i = 0; a[i] != '\0'; i++) {
		cout << a[i];
	}
}