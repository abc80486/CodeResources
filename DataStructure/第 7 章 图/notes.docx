1* 图中的数据元素通常称为顶点，两个数据元素之间的关系用弧和边表示
初始点也叫弧尾，终端点称为弧头；
2* 每两个顶点都存在边的图称为完全图，具有n(n-1)条弧称为有向完全图；
3* 当边或者弧的数目少于nlogn(n代表顶点数)称为稀疏图，反之称为稠密
树；
4* 与图的边或弧相关的数叫做权，带权的图通常称为网；
5*顶点的度是与顶点相关联的边的数目，记为TD（V）；
6*入度ID(V),出度OD(V)；
7*边或弧的数目等于各个顶点度的数目的和的一半；
8*路径是指一个顶点到另一个顶点之间的顶点序列；
9*序列中的顶点不重复出现的路径称为简单路径；
10*如果无向图中的任意两个顶点都连通，称为连通图；
11*连通分量：无向图中极大连通子图；
12*在有向图中，如果任意两个顶点之间双向都存在路径，则成为强连通图； 
13*一个连通图的生成树是一个极小连通子图，包含n个顶点n-1条边；而且有且只有n-1条边；
14*如果一个有向图恰好有一个顶点的顶点的入度为0，其余顶点的入度为1，则是一颗有向数。
一个有向图的生成森林由若干棵有向树组成，含有图中全部顶点。
