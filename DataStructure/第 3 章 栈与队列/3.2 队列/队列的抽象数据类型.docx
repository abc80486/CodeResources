ADT Queue{
    数据对象：D={ ai|ai<-ElemType ,i=1,2,...,n ,n>=0}
    数据关系：R1={<a(i-1),ai>|a(i-1),ai<-D,i=1,2,...,n}
    j基本操作：
        InitQueue(&Q)
        DestroyQueue(&Q)
        ClearQueue(&Q)
        QueueEmpty(Q)
        QueueLength(Q)
        GetHead(Q,&e)
        EnQueue(&Q,e)
        DeQueue(&Q,&e)
        QueueTraverse(Q,visit())
}